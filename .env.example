# Optional backend endpoint for a future AI or finance API.
VITE_ARTHIQ_API_URL=
# Use only a public client-side key. Keep private secrets on a server.
VITE_ARTHIQ_API_KEY=

# Supabase placeholders. Use only the public anon key in the browser.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
